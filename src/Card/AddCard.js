import React from 'react';
//Routes to '/decks/:deckId/cards/new'
//Allows the user to add a new card to an existing deck

function AddCard() {
    return (
        <div>Add card component</div>
    )
}

export default AddCard;